# This function takes two numbers as input and returns their sum.
def add_numbers(a, b):
    """
    This function adds two numbers together and returns the result.

    Parameters:
        a (int): The first number.
        b (int): The second number.

    Returns:
        int: The sum of the two input numbers.
    """
    # Calculate the sum of the numbers.
    sum_result = a + b
    
    # Return the sum to the caller.
    return sum_result

# Call the function and store the result in a variable.
result = add_numbers(3, 5)

# Print the result.
print("The sum is:", result)

'''In Python, the return statement is used to exit a function and return a value back to the caller. It allows a function to provide an output or result that can be used in further computations or operations.
'''